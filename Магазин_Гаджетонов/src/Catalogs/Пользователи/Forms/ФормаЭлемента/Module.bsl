
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТекПользователь = ПользователиИнформационнойБазы.НайтиПоИмени(Объект.Наименование);
	
	Объект.ТаблицаПрав.Очистить();
	
	НаборПрав = Метаданные.Роли;
	
	Для Каждого Роль Из НаборПрав Цикл
		СтрокаТЧ = Объект.ТаблицаПрав.Добавить();
		СтрокаТЧ.ИмяРоли = Роль.Имя;
		СтрокаТЧ.Включена = ТекПользователь.Роли.Содержит(Роль);
	КонецЦикла;
	
КонецПроцедуры
